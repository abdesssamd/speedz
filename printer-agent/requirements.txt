requests==2.32.3
pywin32==308
win10toast==0.9
pyinstaller==6.15.0
# Optionnels (degradation gracieuse si absents) :
pystray==0.19.5          # icone barre des taches (system tray)
Pillow==10.4.0           # icone tray + logo raster sur les tickets
websocket-client==1.8.0  # reception temps reel des commandes
